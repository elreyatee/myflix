%section.invite.container
  .row
    .col-sm-10.col-sm-offset-1
      = bootstrap_form_for @invitation, class: "invite" do |f|
        %header
          %h1 Invite a friend to join MyFlix!
        %fieldset
          .form-group
            .row
              .col-sm-4
                = f.text_field :name, class: "form-control", label: "Friend's Name"
          .form-group
            .row
              .col-sm-4
                = f.email_field :email, class: "form-control", label: "Friend's Email Address"
          .form-group
            .row
              .col-sm-6
                = f.text_area :message, rows: 6, class: "form-control", placeholder: "Please join this really cool site!", label: "Invitation Message"
        %fieldset.form-group.action
          = submit_tag "Send Invitation", class: "btn btn-default"
